<header class="site-header">
	<a href="/" class="brand" aria-label="naihood home">nai<span>hood</span></a>
	<div class="location">📍 Kitale HQ</div>
	<a href="/me" class="profile" aria-label="Open profile">◎</a>
</header>

<style>
	.site-header {
		position: sticky;
		top: 0;
		z-index: 20;
		display: flex;
		align-items: center;
		justify-content: space-between;
		height: 3.5rem;
		padding: 0 1.5rem;
		border-bottom: 1px solid rgba(255, 255, 255, 0.06);
		background: rgba(0, 0, 0, 0.6);
		backdrop-filter: blur(24px);
	}

	.brand {
		color: white;
		font-size: 1.25rem;
		font-weight: 900;
		letter-spacing: -0.08em;
		text-decoration: none;
	}

	.brand span {
		color: #8b5cf6;
	}

	.location {
		padding: 0.3rem 0.75rem;
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.05);
		color: #d4d4d8;
		font-size: 0.75rem;
	}

	.profile {
		display: grid;
		width: 2rem;
		height: 2rem;
		place-items: center;
		border: 1px solid rgba(255, 255, 255, 0.1);
		border-radius: 50%;
		color: #d4d4d8;
		text-decoration: none;
	}

	@media (max-width: 480px) {
		.site-header {
			padding: 0 1rem;
		}
	}
</style>
